
# mysql properties
# to add to below? /demo?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
### when beginning multiple databases, need to use spring.datasource. (for the first one) then spring.second-datasource. (for the second)
spring.datasource.url= jdbc:mysql://localhost:3306/rollordie
spring.datasource.username= root
spring.datasource.password= root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.generate-ddl=true
spring.jpa.show-sql=true

# mongodb properties
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=rollordie

server.port = 8080